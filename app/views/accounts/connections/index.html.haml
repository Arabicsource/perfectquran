= render 'shared/page_header', title: 'Connections'

%section.section
  .container
    .columns
      .column
        = render 'accounts/shared/menu'
      .column.is-three-quarters
        %section#connections
          - if @connections.any?
            %h1.title Connections            
            %table.table.is-fullwidth
              %thead
                %tr
                  %th UID
                  %th Name
                  %th Provider
                  %th Status
                  %th
              %tfoot
                %tr
                  %th UID
                  %th Name
                  %th Provider
                  %th Status
                  %th
              %tbody
                - @connections.each do |connection|
                  %tr{id: "connection-#{connection.id}"}
                    %td= link_to connection.provider_uid, edit_accounts_connection_path(connection)
                    %td= connection.name
                    %td= connection.provider
                    %td= connection.status
                    %td
                      = link_to accounts_connection_path(connection), method: :delete, class: 'button is-danger' do
                        #{fa_icon('trash')} Delete

        = link_to '/auth/twitter', class: 'is-twitter-button' do
          #{fa_icon('twitter')} Connect Twitter Account

- provide :title, 'Connections'