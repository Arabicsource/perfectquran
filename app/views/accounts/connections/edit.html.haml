- content_for :header do
  = render 'accounts/shared/menu'

- content_for :main do
  = render 'form'

- content_for :aside do
  = render 'automatic_posts_message'  

= render "layouts/templates/is_two_thirds", title: 'Edit Connection'