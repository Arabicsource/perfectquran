- @posts.each do |post|
  %article.post
    %h2
      = post.title
    
    %p.post-meta
      %small
        Posted in
        = post.category.name
    
    .post-content
      = markdown post.content

- provide :title, 'Blog'