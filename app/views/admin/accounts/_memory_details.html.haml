- if @account.memories.any?
  %section
    %h1.title Memories
    %table.table.is-fullwidth.is-bordered
      %tbody
        - @account.memories.each do |memory|
          %tr
            %td= memory.id
            %td= memory.ayah_id