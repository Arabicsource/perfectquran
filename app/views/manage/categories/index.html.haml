%table.table
  %thead
    %tr
      %th #
      %th Name
  %tbody
    - @categories.each do |category|
      %tr
        %td= category.id
        %td= category.name
           
- provide :title, 'Categories'