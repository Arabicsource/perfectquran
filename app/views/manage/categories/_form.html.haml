= form_with model: [:manage, @category], id: 'category-form' do |f|

  = render 'shared/error_messages', object: @category
  
  .form-group
    = f.label :name
    = f.text_field :name, id: :category_name, class: 'form-control'

  = f.submit class: 'btn btn-primary'