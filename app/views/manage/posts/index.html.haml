%table.table
  %thead
    %tr
      %th #
      %th Title
  %tbody
    - @posts.each do |post|
      %tr
        %td
          = post.id
        %td
          = post.title
          
- provide :title, 'Posts'